# 🚀 গিটহাব ও ১০০% ফ্রি হোস্টিং গাইড (Free Hosting Guide)

আপনার প্রজেক্টটি গিটহাবে আপলোড করার সাথে সাথে যেন এটি সম্পূর্ণ ফ্রিতে ইন্টারনেটে লাইভ হোস্টিং হয়ে যায়, তার জন্য সব ধরনের প্রয়োজনীয় কনফিগারেশন ফাইল (`vercel.json`, `netlify.toml`, `.github/workflows/deploy.yml`, `render.yaml`) তৈরি করে দেওয়া হয়েছে।

---

## 🌟 পদ্ধতি ১: Vercel দিয়ে অটোমেটিক ফ্রি হোস্টিং (সবচেয়ে সেরা ও সহজ)
*Vercel শুধুমাত্র ১ মাসের জন্য নয়, এটি আজীবনের জন্য ১০০% ফ্রি এবং বিশ্বমানের হাই-স্পিড সিডিএন প্রদান করে।*

### ধাপসমূহ:
1. **গিটহাবে আপলোড করুন:**
   - [github.com](https://github.com)-এ যান এবং একটি নতুন রেপোজিটরি (New Repository) তৈরি করুন (নাম দিন যেমন: `my-3d-admin-panel`)।
   - আপনার কম্পিউটারের এই ফোল্ডারের ফাইলগুলো গিটহাবে আপলোড করুন (বা `git push` করুন)।

2. **Vercel-এ কানেক্ট করুন:**
   - [vercel.com](https://vercel.com)-এ যান এবং **"Continue with GitHub"** দিয়ে ফ্রি সাইন-আপ করুন।
   - ড্যাশবোর্ডে **"Add New..."** -> **"Project"** বাটনে ক্লিক করুন।
   - আপনার গিটহাবের `my-3d-admin-panel` রেপোটি দেখতে পাবেন, পাশে থাকা **"Import"** বাটনে চাপ দিন।
   - কোনো সেটিং পরিবর্তন করার প্রয়োজন নেই (আমরা ইতিমধ্যে `vercel.json` কনফিগার করে রেখেছি)।
   - সরাসরি **"Deploy"** বাটনে ক্লিক করুন!

3. **রেজাল্ট:**
   - ৩০ সেকেন্ডের মধ্যে আপনার প্রজেক্ট সম্পূর্ণ লাইভ হয়ে যাবে এবং আপনি একটি ফ্রি পাবলিক ডোমেন পাবেন (যেমন: `https://my-3d-admin-panel.vercel.app`)।
   - **ম্যাজিক:** এরপর আপনি গিটহাবে কোনো ফাইল পরিবর্তন করে সেভ করলেই Vercel স্বয়ংক্রিয়ভাবে লাইভ সাইট আপডেট করে দেবে!

---

## 🌟 পদ্ধতি ২: সরাসরি GitHub Pages-এ ফ্রি হোস্টিং (কোনো ওয়েবসাইট অ্যাকাউন্ট ছাড়াই)
*গিটহাব নিজেই সম্পূর্ণ ফ্রিতে ওয়েবসাইট হোস্ট করে দেয়।*

### ধাপসমূহ:
1. আপনার গিটহাব রেপোজিটরিতে যান।
2. উপরের মেনু থেকে **Settings** ট্যাবে ক্লিক করুন।
3. বাম পাশের মেনু থেকে **Pages** অপশনে যান।
4. **Build and deployment** সেকশনে **Source** ড্রপডাউন থেকে **"GitHub Actions"** সিলেক্ট করুন।
5. ব্যস! আমরা আগেই `.github/workflows/deploy.yml` তৈরি করে দিয়েছি। গিটহাব প্রতিবার কোড পাওয়ার সাথে সাথে একা একাই বিল্ড করে লাইভ লিংক তৈরি করে দেবে:
   `https://<your-username>.github.io/<repo-name>/`

---

## 🌟 পদ্ধতি ৩: Render.com দিয়ে ব্যাকএন্ড এপিআই ও ডিসকর্ড বট হোস্টিং (ফ্রি)
*যদি আপনি চান আপনার পাইথন বট এবং ব্যাকএন্ড এপিআই ২৪ ঘণ্টা ফ্রিতে ক্লাউডে রান হোক:*

1. [render.com](https://render.com)-এ একটি ফ্রি অ্যাকাউন্ট খুলুন।
2. **New +** বাটনে ক্লিক করে **"Blueprint"** সিলেক্ট করুন।
3. আপনার গিটহাব রেপো কানেক্ট করুন। রেন্ডার স্বয়ংক্রিয়ভাবে আমাদের তৈরি করা `render.yaml` ফাইলটি পড়বে এবং এপিআই সার্ভার ও ফ্রন্টএন্ড হোস্ট করে দেবে।
4. Environment Variables-এ আপনার `DISCORD_BOT_TOKEN` দিয়ে দিলেই বট ক্লাউডে ২৪/৭ চলতে থাকবে।

---

## 📁 কোন ফাইলগুলো আমরা রেডি করে দিয়েছি:
1. `vercel.json` - Vercel-এ ১-ক্লিকে ডেপ্লয়মেন্টের জন্য।
2. `netlify.toml` - Netlify-এ হোস্টিংয়ের জন্য।
3. `.github/workflows/deploy.yml` - গিটহাব অ্যাকশন দিয়ে সরাসরি গিটহাব পেজে ডেপ্লয়ের জন্য।
4. `render.yaml` & `Procfile` - পাইথন ব্যাকএন্ড ও বটের ক্লাউড হোস্টিংয়ের জন্য।
5. `requirements.txt` - ক্লাউড সার্ভারে পাইথন ডিপেন্ডেন্সি ইনস্টল করার জন্য।
6. `.gitignore` - আপনার সিক্রেট টোকেন ও অপ্রয়োজনীয় বড় ফাইল গিটহাবে যাওয়া আটকাতে।
